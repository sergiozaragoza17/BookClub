{% extends 'base.html.twig' %}

{% block title %}Manage Users{% endblock %}

{% block body %}
    <h1>Manage Users</h1>

    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Name</th>
            <th>Roles</th>
            <th>Joined At</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.name }}</td>
                <td>{{ user.roles|join(', ') }}</td>
                <td>{{ user.joinedAt ? user.joinedAt|date('Y-m-d') : '' }}</td>
                <td>
{#                    <a href="{{ path('admin_user_show', {id: user.id}) }}">View</a>#}
                    <a href="{{ path('admin_user_edit', {id: user.id}) }}">Edit</a>
                    <form method="post" action="{{ path('admin_user_delete', {id: user.id}) }}" style="display:inline;" onsubmit="return confirm('Are you sure?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">No users found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}